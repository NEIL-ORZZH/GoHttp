<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.xiaopan.android.gohttp.sample">

    <!-- 声明要使用的权限 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- 声明应用需要的各种组件 -->
    <application android:name=".MyApplication" android:allowBackup="true" android:icon="@drawable/ic_launcher" android:label="@string/app_name" android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity" android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.BinaryActivity" android:label="@string/activityTitle_binary"/>
        <activity android:name=".activity.StringActivity" android:label="@string/activityTitle_string"/>
        <activity android:name=".activity.JsonActivity" android:label="@string/activityTitle_json"/>
        <activity android:name=".activity.DownloadActivity" android:label="@string/activityTitle_download"/>
        <activity android:name=".activity.RequestObjectActivity" android:label="@string/activityTitle_requestObject"/>
    </application>
</manifest>